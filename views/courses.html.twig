<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Courses</title>
    </head>
    <body>
        <h1>Courses</h1>

        {% if courses is not empty %}
            <p>Here are all your courses:</p>
            <ul>
                {% for course in courses %}
                    <li>
                        <a href="/course/{{ course.getId }}">{{ course.getName }} {{ course.getCourseNumber }}</a>
                        <form action='/course/{{ course.getId }}/edit' method='get'>
                            <button type='submit'>Edit Course</button>
                        </form>
                        <form action="/course/{{ course.getId }}/delete" method="post">
                            <input name="_method" type="hidden" value="delete">
                            <button type="submit">Delete </button>
                        </form>
                    </li>
                {% endfor %}
        {% endif %}

            </ul>



        <p><a href='/'>Home</a></p>
    </body>
</html>
